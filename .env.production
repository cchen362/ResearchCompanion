# Production environment variables
# These values are used when building for production

# API Configuration
VITE_API_BASE_URL=/api
VITE_USE_SERVER_STORAGE=true

# Note: Other sensitive variables (API keys) should be set via Docker environment variables
# and not committed to version control